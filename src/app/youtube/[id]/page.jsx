import clsx from 'clsx';
import styles from './detail.module.scss';

export default function Detail({ params }) {
	return (
		<div className={clsx(styles.detail)}>
			<h1>Youtube Detail</h1>
		</div>
	);
}
